<div id="background">
    <div id="blog">
        {{#if dbError}}
        <p>Sorry, there was an error with the database. Please try again later.</p>
        {{else}}
        <h2>Edit this blogpost here: ? (only for me)</h2>
        <form class="form-title" method="post" action="/edit-blogpost/{{blog.id}}">
            <label for="blog-title">Title:</label>
            <input type="text" id="blog-title" value="{{blog.title}}" name="blog-title" required>

            <label for="blog-text">Write what's on your mind:</label>
            <textarea name="blog-text" id="blog-text" cols="30" rows="10" required>{{blog.content}}</textarea>

            <button type="submit" id="enter-blog">Enter blog</button>
        </form>
        {{/if}}
    </div>
</div>